import React from "react";

const ProblemSection = () => {
    return (
        <section className="py-16 px-4 md:px-8">
            <div className="max-w-5xl mx-auto text-center">
                <h2 className="text-2xl  md:text-3xl font-serif text-neutral-900 mb-5">
                    We get it — most CRMs suck, clunky, Overpriced?
                </h2>
                <h2 className="text-2xl md:text-3xl font-light text-neutral-900 mb-6 sm:mb-10">
                    Our CRM simplifies client tracking,
                    automates workflows, and just works.
                </h2>
            </div>
        </section>
    );
};

export default ProblemSection;
